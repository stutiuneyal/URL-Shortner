spring.application.name=urlshortner

spring.data.mongodb.uri=${MONGO_URI:mongodb://root:password@localhost:27017/urlshortner?authSource=admin}
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6380}

server.port=8091

# <class>.<instance variable as a class>.<nstance variable as a class>.<instance-variable> = <value>
app.base-url=${BASE_URL:http://localhost:8091}
app.public-web-url=${PUBLIC_WEB_URL:http://localhost:5173}

app.jwt.secret=${JWT_SECRET:ewkjbfiwe237r62378hdbcyuwegr734@&!^%E^&!@TE^&@$^!@vdqvuqytf63&T&^T}
app.jwt.access-ttl-min=${JWT_ACCESS_TTL_MIN:15}
app.jwt.refresh-ttl-days=${JWT_REFRESH_TTL_DAYS:30}

# Enabling all endpoints
app.cors.origins="*"


app.rate.window-seconds=${RATE_WINDOW_SECONDS:60}
app.rate.max-hits=${RATE_MAX_HITS:120}

# Country analytics
app.geo.enabled=${GEO_ENABLED:false}
app.geo.database-path=${GEO_DB_PATH:./geoip/GeoLite2-Country.mmdb}


